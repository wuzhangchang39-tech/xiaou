const reveals=document.querySelectorAll(".reveal");const io=new IntersectionObserver((entries)=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add("show")})},{threshold:.18});reveals.forEach(el=>io.observe(el));const pinImg=document.querySelector(".pin-img");const hero=document.querySelector(".hero-product");window.addEventListener("scroll",()=>{const y=window.scrollY;if(hero){hero.style.transform=`translateY(${Math.min(y*.08,70)}px) scale(${Math.max(1-y/6000,.92)})`;}if(pinImg){const wrap=document.querySelector(".pin-wrap");const rect=wrap.getBoundingClientRect();const progress=Math.min(Math.max(-rect.top/(wrap.offsetHeight-innerHeight),0),1);pinImg.style.transform=`scale(${.82+progress*.25}) rotate(${-2+progress*2}deg)`;pinImg.style.filter=`brightness(${.82+progress*.18})`;}});